apiVersion: v1
kind: Pod
metadata:
  name: python
spec:
  nodeSelector:
  containers:
    - name: python
      image: avga9935/cisco:1
      
      # command: [ "/bin/bash", "-c", "--" ]
      # args: [ "while true; do sleep 30; done;" ]